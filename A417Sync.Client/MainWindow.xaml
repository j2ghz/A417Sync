<Window x:Class="A417Sync.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:A417Sync.Client"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525"
        Name="Main"
        d:DataContext="{d:DesignInstance local:MainWindowViewModel }">
    <TabControl>
        <TabItem Header="Main">
            <StackPanel Orientation="Horizontal">

                <StackPanel Width="227.332">
                    <ComboBox Grid.Row="2" Margin="0,0,0,25">
                        <ComboBoxItem IsSelected="True">(Everything)</ComboBoxItem>
                    </ComboBox>
                    <Button Grid.Column="0" Click="LoadRepo" IsEnabled="{Binding CanLoadRepo, Mode=OneWay}" Height="64.991">Check</Button>
                    <Button Grid.Column="1" Click="Download" IsEnabled="{Binding CanDownload, Mode=OneWay}" Height="57.303">Update</Button>
                    <Button Click="Start" IsEnabled="{Binding CanStart, Mode=OneWay}" Height="58.401" Content="Start" VerticalContentAlignment="Center"/>
                </StackPanel>
                <TextBlock Text="{Binding DownloadInfo}" Width="208.783" Margin="75,0,0,0" />
            </StackPanel>
        </TabItem>
        <TabItem Header="Settings/Debug">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="AUto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Menu Grid.Row="0">
                    <MenuItem Header="Debug">
                        <MenuItem Header="Toggle Console" Click="ConsoleToggle" />
                    </MenuItem>
                    <MenuItem Header="Feedback" Click="Feedback"/>
                </Menu>
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBox Grid.Column="0" Grid.Row="0" Padding="5" Text="{Binding Url, Mode=TwoWay}"/>
                    <TextBox Grid.Column="0" Grid.Row="1" Padding="5" Text="{Binding Path, Mode=TwoWay}"/>
                </Grid>
                <ListView Grid.Row="3" ItemsSource="{Binding Actions}">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Path" DisplayMemberBinding="{Binding Path, Mode=OneWay}" Width="Auto"/>
                            <GridViewColumn Header="Size" DisplayMemberBinding="{Binding Size, Mode=OneWay}" Width="Auto"/>
                            <GridViewColumn Header="Action" DisplayMemberBinding="{Binding Action, Mode=OneWay}" Width="Auto"/>
                            <GridViewColumn Header="Progress" Width="300">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <ProgressBar Value="{Binding Progress, Mode=OneWay}"/>
                                            <TextBlock Text="{Binding Progress, Mode=OneWay, StringFormat={}{0:0}%}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Grid>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                    <ListView.ItemTemplate>
                        <DataTemplate />
                    </ListView.ItemTemplate>
                </ListView>
                <Grid Row="4">
                    <ProgressBar MinHeight="40" Value="{Binding Progress, Mode=OneWay}"/>
                    <TextBlock Text="{Binding Progress, Mode=OneWay, StringFormat={}{0:0}%}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Grid>
                <Grid Grid.Row="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Click="LoadRepo" IsEnabled="{Binding CanLoadRepo, Mode=OneWay}">Check</Button>
                    <Button Grid.Column="1" Click="Download" IsEnabled="{Binding CanDownload, Mode=OneWay}">Update</Button>
                    <Button Grid.Column="2" Click="Start" IsEnabled="{Binding CanStart, Mode=OneWay}">Start</Button>
                </Grid>
            </Grid>
        </TabItem>
    </TabControl>
</Window>